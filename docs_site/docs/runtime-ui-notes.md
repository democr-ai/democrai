# Runtime UI Notes

- Documentare il funzionamento di `stream_id`, che viene iniettato automaticamente nelle action.
- Quando si chiede un esempio SDUI completo, specificare i file minimi attesi: pagina Python che carica lo YAML, file YAML con la UI completa e file action. Questo evita risposte con soli frammenti di componenti o sole action.
